const config = {
  theme: {
    extend: {
      fontFamily: {
        Outfit: ["Outfit", "sans-serif"],
        Ovo: ["Ovo", "serif"],
      },
    },
  },

  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
